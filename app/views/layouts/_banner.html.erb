<div class="container">
  <div class="col-md-7 banner-inner-wrapper">
    <div class="banner-text">
      <% @pages.each do |page| %>
        <h1><%= page.title %></h1>
        <p><%= simple_format page.body %></p>
      <% end %>
  </div>
</div>
